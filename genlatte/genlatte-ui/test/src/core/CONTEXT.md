# Core Logic Tests

**Purpose:**
Tests the foundational systems of the application, including routing and global configurations.

**Subdirectories:**
- `routing/`: Redirection and access control verification.

**Dependencies/Relationships:**
- Validates logic in `genlatte-ui/lib/src/core/`.
