# Recent Orders Feature Tests

**Purpose:**
Tests for the physics-based "Recent Orders" display logic.

**Subdirectories:**
- `models/`: Physics calculation verification.

**Dependencies/Relationships:**
- Validates logic in `genlatte-ui/lib/src/screens/recent_orders/`.
