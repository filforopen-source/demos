# Queue Home Tests

**Purpose:**
Unit tests for the autonomous Queue display logic.

**Detailed File Overviews:**
- `queue_home_bloc_test.dart`: Exercises the `QueueHomeBloc`, specifically testing its deterministic sharding logic and pagination behavior.

**Dependencies/Relationships:**
- Validates the BLoC in `genlatte-ui/lib/src/screens/queue/home/`.
