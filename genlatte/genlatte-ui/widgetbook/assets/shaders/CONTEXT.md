# Widgetbook Shader Assets

**Purpose:**
Mirrors the application's shader assets for use within Widgetbook component previews.

**Detailed File Overviews:**
- `squish.glsl`: Used to preview shader-based effects (like `SquishWidget`) in Widgetbook.

**Dependencies/Relationships:**
- Referenced by `genlatte-ui/widgetbook/lib/squish_widget_preview.dart`.
