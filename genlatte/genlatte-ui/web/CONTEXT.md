# Web Configuration & Entry

**Purpose:**
Contains the root assets and configurations for the Flutter Web build target.

**Subdirectories:**
- `icons/`: PWA and browser icon assets.

**Dependencies/Relationships:**
- Contains `index.html` and `manifest.json` which define the web application's metadata and entry point.
